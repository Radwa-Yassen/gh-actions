name: First Workflow
on: workflow_dispatch  #when to run also with this we can run it manually
jobs:
  first-job:
    runs-on: ubuntu-latest # the env. to run on
    steps:
      - name: print greeting
        run: echo "Hello world!"
      - name: to run 2 lines
        run: |
            echo "first o/p"
            echo "second o/p"
      - name: print goodbye
        run: echo "Done - bye!"
